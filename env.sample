# Copy this file to .env and fill in your values.
# .env is gitignored — never commit it.
#
# Usage: all scripts in scripts/ source .env automatically if present.
# Or export variables in your shell before running scripts.

STACK_NAME=hello-world-test
AWS_REGION=us-east-1
APP_IMAGE=vineetma/ecs-hello-world:1.4
AMI_ID=ami-0dc67873410203528   # Starting point — oldest ECS-optimized AL2. Use update-ami.sh to upgrade.
